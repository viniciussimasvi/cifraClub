import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#fff",
    width: "100%",
    height: 45,
    borderRadius: 5,
    paddingLeft: 10,
    fontSize: 16,
    borderWidth: 1,
    borderColor: "#aca9a9",
    borderStyle: "solid",
  },
});
