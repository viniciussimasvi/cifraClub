import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 10,
  },

  image: {
    width: 340,
    height: 350,
    borderRadius: 10,
    marginLeft: 20,
    marginRight: 5,
  },
});
